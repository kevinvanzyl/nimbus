<div class="input-group">
	<span class="input-group-addon">
		<span class="glyphicon glyphicon-search"></span>
	</span>
	<input ng-model="query" type="text" class="form-control" placeholder="Search">
	<span class="input-group-btn">
		<a href="#/contacts/find" class="btn btn-primary form-control" role="button"><span class="glyphicon glyphicon-plus"></span></a>
	</span>
</div><br/>
<div class="row">
<ul class="list-group">
	<li class="list-group-item" ng-repeat="contact in contacts | filter:query | orderBy:orderProp" >
		<div class="row">
			<span class="pull-left">
				<p><a href="#/map/{{contact.id}}"><img class="glyphicon-profile" src="{{contact.icon}}" /></a><span class="space-l5">{{contact.name}}</span></p>
			</span>
			<span class="pull-right">
				<p><a class="btn btn-primary form-control sharebtn" ng-class="contact.btnClass" role="button" ng-click="updateSharing(contact.shareBtnMethod, contact.id)">{{contact.shareBtnText}}</a></p>
				<br/><br/><p><small class="statustext">{{contact.shareStatus}}</small></p>
			</span>
			
		</div>
		
	</li>
</ul>
</div>
